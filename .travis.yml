language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-talis-skeletor' ]; then cd .. && eval "mv $currentfolder generator-talis-skeletor" && cd generator-talis-skeletor; fi

